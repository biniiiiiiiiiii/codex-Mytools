目标

交付一个纯前端的 JSON → C# Class 转换器，满足开发过程中的快速建模需求。

本轮范围

做：

JSON 输入

类名配置

命名空间配置

C# 类输出

嵌套对象与数组

基础类型推断

属性映射可选开关

本地保存

复制 / 示例 / 清空

基础测试

不做：

Diff

Viewer

JSONPath

TS Interface

文件导入

多序列化框架兼容

验收标准

做到这些就算第二轮完成：

合法 JSON 能生成可读的 C# 类

支持嵌套对象

支持数组

支持常见基础类型推断

非法 JSON 有提示

可复制结果

刷新后恢复输入和配置

核心推断逻辑有测试